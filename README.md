# Student Progress Mentoring System - SPMS
## How to run:-
1. Open a terminal and run `cd backend`, then write `node server.js`.
2. Open another terminal, this time run `cd frontend`, then write `npm start`.
