## StudentMentoringSystem-frontend
## How to setup
in project directory run `npm install`
then run `npm start`


# !! if your on the testing branch:
# This branch is for testing only, push code into this branch, then if it's confirmed to work on both devices. Push to main branch.
#!!
